// Package rest contains the RESTful HTTP controllers for handling various API endpoints in the playerService.
// It consists of sub-packages for different RESTful endpoints.

// Sub-packages:
// The ping package contains a controller for the ping endpoint, which is used for checking the service's availability.

// The player package includes controllers for handling player-related endpoints, such as retrieving player information,
// creating new players, updating existing players, and deleting players.

// The version package provides a controller for handling the version endpoint, which is used for retrieving the version
// information of the playerService.

package rest